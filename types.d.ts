/*defined my InterviewQuestions type here that my tsconfig.json file can read. id is basicaly the file name and the title is the question itself. */
type InterviewQuestions = {
  id: string;
  title: string;
};
