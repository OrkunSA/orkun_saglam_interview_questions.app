//created simple notFound component that displays a message when the requested question doesn't exist.
export default function NotFound() {
  return <h1>The requested question does not exist.</h1>;
}
